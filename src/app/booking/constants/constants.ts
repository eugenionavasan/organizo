import { ServiceType } from '../types/types';

export const SERVICE_PRICES: Record<ServiceType, number> = {
  haircut: 25,
  haircoloring: 60,
  hairwashing: 15,
  beardtrim: 20,
  scalpmassage: 30,
};